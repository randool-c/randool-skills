# 索引重建

当索引与实际笔记不同步时，全量重建所有 `_index.md`。

## 执行逻辑

1. 从 `<vault>/代码解读/` 开始递归扫描
2. 对每个包含 `.md` 文件或子目录的目录生成 `_index.md`
3. 读取笔记 frontmatter 和核心内容提炼摘要
4. 自底向上生成，确保笔记数统计准确
5. 最后更新 vault 根 `_index.md` 中 `代码解读/` 的条目

**进度输出**：每处理完一个目录输出进度。

## 触发方式

- 用户说"重建索引"
- 检索时发现大量目录缺少 `_index.md`，主动建议重建
